<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/roomTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Room Title"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#000000" />

    <ImageView
        android:id="@+id/roomImage"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_below="@id/roomTitle"
        android:scaleType="centerCrop"
        android:src="@drawable/room_image" />

    <TextView
        android:id="@+id/roomDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/roomImage"
        android:layout_marginTop="10dp"
        android:text="Room Description"
        android:textSize="16sp"
        android:textColor="#333333" />

    <Button
        android:id="@+id/checkAvailabilityButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/roomDescription"
        android:layout_marginTop="20dp"
        android:text="Check Availability" />

    <!-- New Button to Book Room -->
    <Button
        android:id="@+id/bookRoomButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/checkAvailabilityButton"
        android:layout_marginTop="10dp"
        android:text="Book Room" />

    <!-- New TextView for Room Amenities -->
    <TextView
        android:id="@+id/roomAmenities"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/bookRoomButton"
        android:layout_marginTop="20dp"
        android:text="Amenities: Wi-Fi, Air Conditioning, Breakfast Included"
        android:textSize="16sp"
        android:textColor="#333333" />
</RelativeLayout>